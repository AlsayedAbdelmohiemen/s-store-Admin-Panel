enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  cancelled,
}

enum ProductType {
  single,
  variable,
}

enum MediaCategory {
  folders,
  banners,
  brands,
  categories,
  products,
  users,
}
