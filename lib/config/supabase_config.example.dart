/// Example Supabase Configuration Template.
/// 
/// Instructions:
/// 1. Copy this file and rename the copy to: `supabase_config.dart` (in the same directory).
/// 2. Replace the placeholder values below with your actual Supabase project URL and anon/publishable key.
/// 3. Note that `supabase_config.dart` is ignored by git so your secrets remain safe.
class SupabaseConfig {
  static const String url = 'https://YOUR_PROJECT_ID.supabase.co';
  static const String publishableKey = 'YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY';
}
